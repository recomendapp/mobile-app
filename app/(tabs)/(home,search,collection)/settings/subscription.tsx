import { ThemedText } from "@/components/ui/ThemedText";
import Animated from "react-native-reanimated";

const SubscriptionSettings = () => {
	return (
		<Animated.ScrollView>
			<ThemedText>Subscription settings</ThemedText>
		</Animated.ScrollView>
	)
};

export default SubscriptionSettings;